import Link from 'next/link';

export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  return <main dir="rtl" className="min-h-screen bg-[#030712] text-white luxury-grid"><header className="border-b border-white/10"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5"><Link href="/" className="font-extrabold">Louay <span className="text-sky-400">Phone</span></Link><Link href="/products" className="text-sm text-slate-400">الهواتف</Link></div></header><section className="mx-auto max-w-7xl px-5 py-14"><div className="grid gap-8 lg:grid-cols-2"><div className="glass flex min-h-[520px] items-center justify-center rounded-[2rem]"><div className="h-80 w-44 rounded-[2.5rem] border-8 border-slate-700 bg-slate-950 shadow-2xl shadow-sky-500/10" /></div><div className="flex flex-col justify-center"><p className="text-sm font-bold text-sky-400">PRODUCT</p><h1 className="mt-3 text-4xl font-extrabold">{decodeURIComponent(slug)}</h1><p className="mt-6 leading-8 text-slate-400">سيتم تحميل تفاصيل الهاتف الحقيقية من Supabase: الصور، السعر، المواصفات، التوفر، والتقسيط الذي يحدده المسؤول.</p><div className="mt-8 rounded-2xl border border-white/10 bg-white/[.03] p-5"><p className="text-sm font-bold text-sky-300">التقسيط</p><p className="mt-2 text-sm text-slate-400">تظهر خطط الأشهر أولًا، ولا تظهر الدفعة والقسط إلا بعد اختيار المدة.</p><div className="mt-4 flex flex-wrap gap-2">{[3, 6, 12].map(m => <button key={m} className="rounded-xl border border-white/10 px-4 py-2 text-sm hover:border-sky-400/50 hover:text-sky-300">{m} أشهر</button>)}</div></div><button className="mt-6 rounded-2xl bg-sky-400 px-7 py-4 font-extrabold text-slate-950">اطلب الآن</button></div></div></section></main>;
}
